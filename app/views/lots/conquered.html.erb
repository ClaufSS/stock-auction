
<section>
  <h2>Lotes conquistados</h2>

  <div>
    <%= render @conquered_lots %>

    <% if @conquered_lots.empty? %>
      <p>Você não tem lotes conquistados</p>
    <% end %>
  </div>
</section>
