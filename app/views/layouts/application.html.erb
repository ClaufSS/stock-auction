<!DOCTYPE html>
<html>
  <head>
    <title>StockAuction</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class='d-flex w-100 p-2 mx-auto justify-content-between'>
      <%= render 'shared/menu' %>
      <%= render 'shared/account_auth' %>
    </header>

    <div>
      <p><%= notice %></p>
      <p><%= alert %></p>
    </div>
    
    <main>
      <%= yield %>
    </main>
  </body>
</html>
